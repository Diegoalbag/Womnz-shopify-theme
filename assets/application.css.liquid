/*
0 - 600px:      Phone
600 - 900px:    Tablet portrait
900 - 1200px:   Tablet landscape
[1200 - 1800] is where our normal styles apply
1800px + :      Big desktop

$breakpoint arguement choices:
- phone
- tab-port
- tab-land
- big-desktop

ORDER: Base + typography > general layout + grid > page layout > components

1em = 16px
*/
.navbar {
  height: 8rem;
  border-bottom: 1px white solid;
  z-index: 10;
  backdrop-filter: blur(50px); }
  @media only screen and (max-width: 56.25em) {
    .navbar {
      height: 12rem; } }
  .navbar-logo {
    height: 100%;
    width: fit-content;
    margin: 0;
    cursor: pointer; }
    .navbar-logo img {
      height: 100%;
      margin: 0; }
  .navbar-links a {
    font-size: 2rem; }
  .navbar-icons i {
    font-size: 2.5rem; }
    .navbar-icons i:hover {
      cursor: pointer; }
    .navbar-icons i:last-child {
      font-size: 4rem; }
      @media only screen and (max-width: 56.25em) {
        .navbar-icons i:last-child {
          font-size: 7rem; } }

.carousel-content {
  height: 100%;
  width: 100%;
  top: 0;
  padding: 12rem; }
  .carousel-content_topLeft {
    justify-content: flex-start;
    align-items: flex-start; }
  .carousel-content_topRight {
    justify-content: flex-end;
    align-items: flex-start; }
  .carousel-content_bottomLeft {
    justify-content: flex-start;
    align-items: flex-end; }
  .carousel-content_bottomRight {
    justify-content: flex-end;
    align-items: flex-end; }
  .carousel-content_center {
    justify-content: center;
    align-items: flex-end; }
  .carousel-content_title {
    font-size: 10vw;
    white-space: nowrap; }

.collection {
  height: max-content; }
  .collection-card {
    height: 50rem;
    min-height: 400px;
    width: 40rem;
    max-width: 400px;
    min-width: 35rem;
    overflow: hidden; }
    .collection-card_content {
      position: relative;
      background-position: center;
      background-repeat: no-repeat;
      background-size: cover;
      transition: all .2s; }
      .collection-card_content:hover {
        transform: scale(1.2); }
        @media only screen and (max-width: 75em) {
          .collection-card_content:hover {
            transform: none; } }
      @media only screen and (max-width: 75em) {
        .collection-card_content {
          margin: 0 !important; } }

.FeaturedProducts {
  height: min-content;
  padding: 0 8rem;
  overflow: hidden; }
  .FeaturedProducts-slider {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch; }
    .FeaturedProducts-slider__slide {
      scroll-snap-align: start; }

.Product {
  height: 50rem;
  width: 32rem;
  max-height: 500px;
  max-width: 320px; }
  .Product:hover {
    cursor: pointer; }
  @media only screen and (max-width: 37.5em) {
    .Product {
      height: 75rem;
      width: 48rem;
      max-height: 350px;
      max-width: 320px; }
      .Product h2 {
        font-size: 4rem; }
      .Product p {
        font-size: 3rem; } }
  .Product-image img {
    border-radius: .5rem;
    object-fit: cover; }

.CartPage {
  height: min-content; }
  .CartPage-content {
    padding: 11rem 5rem 5rem 5rem;
    min-height: 100vh; }
    @media only screen and (max-width: 56.25em) {
      .CartPage-content {
        padding: 17rem 5rem 5rem 5rem; } }
    @media only screen and (max-width: 37.5em) {
      .CartPage-content_info-subtotal h1, .CartPage-content_info-discount h1, .CartPage-content_info-total h1 {
        font-size: 6rem; }
      .CartPage-content_info-subtotal h4, .CartPage-content_info-discount h4, .CartPage-content_info-total h4 {
        font-size: 4rem; } }

.CartItem {
  height: max-content;
  border-bottom: 1px solid #4c4c4c; }
  .CartItem-image {
    height: 15rem;
    width: 10rem; }
    .CartItem-image img {
      height: 100%;
      width: 100%;
      object-fit: cover; }
    @media only screen and (max-width: 37.5em) {
      .CartItem-image {
        height: 25rem;
        width: 20rem; } }
  @media only screen and (max-width: 37.5em) {
    .CartItem-info_title {
      font-size: 5rem; } }
  @media only screen and (max-width: 37.5em) {
    .CartItem-info_variant {
      font-size: 4rem; } }
  .CartItem-info_totalprice {
    white-space: nowrap; }
    @media only screen and (max-width: 37.5em) {
      .CartItem-info_totalprice {
        font-size: 4rem; } }
  .CartItem-info_finalprice {
    position: relative;
    width: min-content;
    white-space: nowrap; }
    .CartItem-info_finalprice::after {
      content: '';
      position: absolute;
      top: 45%;
      left: 0;
      height: 2px;
      width: 100%;
      background-color: #6c757d; }
    @media only screen and (max-width: 37.5em) {
      .CartItem-info_finalprice {
        font-size: 3rem; } }
  @media only screen and (max-width: 37.5em) {
    .CartItem-info_discountedprice {
      font-size: 3rem; } }
  .CartItem-info_quantity {
    height: 50px;
    min-width: 130px;
    width: min-content;
    padding: 0 1rem;
    background: transparent;
    font-size: 20px; }
    .CartItem-info_quantity input {
      outline: none;
      border: none;
      appearance: none;
      width: min-content; }
    .CartItem-info_quantity input[type=number]::-webkit-inner-spin-button,
    .CartItem-info_quantity input[type=number]::-webkit-outer-spin-button {
      display: none; }
    .CartItem-info_quantity i {
      cursor: pointer; }

.Checkout-button {
  cursor: pointer;
  text-transform: uppercase;
  text-decoration: none;
  text-align: center;
  font-size: 3rem;
  display: inline-flex;
  justify-content: center;
  align-items: center;
  background-color: white;
  color: black;
  padding: 1rem 3rem;
  border: .3rem white solid;
  transition: all .2s;
  white-space: nowrap;
  width: 100%;
  height: 6rem;
  box-shadow: 0 0.5rem 0.5rem rgba(0, 0, 0, 0.4); }
  @media only screen and (max-width: 37.5em) {
    .Checkout-button {
      padding: 2rem 1rem; } }
  @media only screen and (max-width: 37.5em) {
    .Checkout-button {
      padding: 2rem 1rem;
      height: 15rem;
      font-size: 5rem; } }
  .Checkout-button:hover {
    background-color: white;
    color: black;
    transform: translateY(-7px);
    box-shadow: 0 1rem 1rem rgba(0, 0, 0, 0.4);
    text-decoration: none; }
    @media only screen and (max-width: 75em) {
      .Checkout-button:hover {
        transform: none;
        box-shadow: 0 0.5rem 0.5rem rgba(0, 0, 0, 0.4); } }
  .Checkout-button:active {
    transform: translateY(-8px);
    box-shadow: 0 0.5rem 2rem rgba(0, 0, 0, 0.4); }
  .Checkout-button::after {
    content: "";
    display: inline-block;
    height: 100%;
    width: 100%;
    position: absolute;
    top: 0;
    left: 0;
    z-index: -1; }

.Button {
  cursor: pointer;
  text-transform: uppercase;
  text-decoration: none;
  text-align: center;
  font-size: 3rem;
  display: inline-flex;
  justify-content: center;
  align-items: center;
  background-color: white;
  color: black;
  padding: 1rem 3rem;
  border: .3rem white solid;
  transition: all .2s;
  white-space: nowrap;
  width: 50rem;
  height: 8rem;
  box-shadow: 0 0.5rem 0.5rem rgba(0, 0, 0, 0.4); }
  @media only screen and (max-width: 37.5em) {
    .Button {
      padding: 2rem 1rem; } }
  .Button:hover {
    background-color: white;
    color: black;
    transform: translateY(-10px);
    box-shadow: 0 1rem 1rem rgba(0, 0, 0, 0.4);
    text-decoration: none; }
    @media only screen and (max-width: 75em) {
      .Button:hover {
        transform: none;
        box-shadow: 0 0.5rem 0.5rem rgba(0, 0, 0, 0.4); } }
  .Button:active {
    transform: translateY(-8px);
    box-shadow: 0 0.5rem 2rem rgba(0, 0, 0, 0.4); }
  .Button::after {
    content: "";
    display: inline-block;
    height: 100%;
    width: 100%;
    position: absolute;
    top: 0;
    left: 0;
    z-index: -1; }

.ProductPage {
  min-height: 100vh; }
  .ProductPage-content {
    padding-top: 17rem;
    min-height: 100vh; }
    .ProductPage-content__info-title {
      font-size: 5rem;
      font-weight: 300; }
      @media only screen and (max-width: 37.5em) {
        .ProductPage-content__info-title {
          font-size: 8rem; } }
    .ProductPage-content__info-subtitle {
      font-size: 3rem; }
      @media only screen and (max-width: 37.5em) {
        .ProductPage-content__info-subtitle {
          font-size: 5rem; } }
    .ProductPage-content__info-price {
      font-size: 3rem; }
      @media only screen and (max-width: 37.5em) {
        .ProductPage-content__info-price {
          font-size: 5rem; } }
    .ProductPage-content__info-description {
      font-size: 2rem; }
      @media only screen and (max-width: 37.5em) {
        .ProductPage-content__info-description {
          font-size: 4rem; } }
    .ProductPage-content__info-options button {
      height: 4rem;
      min-width: 4rem;
      padding: 0 1rem;
      background: transparent; }
      .ProductPage-content__info-options button p {
        color: white; }
      .ProductPage-content__info-options button:focus {
        outline: none; }
      @media only screen and (max-width: 37.5em) {
        .ProductPage-content__info-options button {
          height: 8rem;
          min-width: 4rem;
          padding: 0 2rem; }
          .ProductPage-content__info-options button p {
            font-size: 5rem;
            font-weight: 300; } }
    .ProductPage-content__info-quantity {
      height: 50px;
      min-width: 130px;
      width: min-content;
      padding: 0 1rem;
      background: transparent;
      font-size: 20px; }
      .ProductPage-content__info-quantity input {
        outline: none;
        border: none;
        appearance: none;
        width: min-content; }
      .ProductPage-content__info-quantity input[type=number]::-webkit-inner-spin-button,
      .ProductPage-content__info-quantity input[type=number]::-webkit-outer-spin-button {
        display: none; }
      .ProductPage-content__info-quantity i {
        cursor: pointer; }
    .ProductPage-content__info-actions span, .ProductPage-content__info-actions i {
      font-size: 2rem; }
      @media only screen and (max-width: 37.5em) {
        .ProductPage-content__info-actions span, .ProductPage-content__info-actions i {
          font-size: 3rem; } }

.ActionButton {
  height: 5rem;
  min-width: 7rem;
  padding: 0 3rem;
  background: transparent; }
  .ActionButton p {
    color: white;
    white-space: nowrap;
    font-size: 2rem; }
  .ActionButton:focus {
    outline: none; }
  .ActionButton:active {
    background: white; }
    .ActionButton:active p {
      color: black; }
  @media only screen and (max-width: 37.5em) {
    .ActionButton {
      height: 10rem; }
      .ActionButton p {
        font-size: 4rem; } }

.selected {
  background-color: white !important; }
  .selected p {
    color: black !important; }

.disabled {
  cursor: not-allowed !important;
  border-color: grey !important; }
  .disabled p {
    color: grey !important; }
  .disabled:active {
    background: transparent !important; }
    .disabled:active p {
      color: grey !important; }

.AddedNotification {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  height: 35rem;
  width: 30rem;
  background-color: #00b600;
  border-radius: 1rem;
  display: flex;
  justify-content: center;
  align-items: center; }
  .AddedNotification i {
    font-size: 15rem; }

.fade-enter-active, .fade-leave-active {
  transition: opacity .2s; }

.fade-enter, .fade-leave-to {
  opacity: 0; }

.CollectionPage {
  min-height: 100vh; }
  .CollectionPage-content {
    padding: 11rem 5rem 5rem 5rem;
    height: min-content; }
    @media only screen and (max-width: 56.25em) {
      .CollectionPage-content {
        padding: 17rem 5rem 5rem 5rem; } }
    .CollectionPage-content_column {
      height: min-content; }

*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: inherit; }

body {
  box-sizing: border-box;
  background-color: #1f1f1f;
  position: relative;
  font-family: 'Jost', sans-serif; }

html {
  font-size: 62.5%;
  -webkit-text-size-adjust: none; }
  @media only screen and (max-width: 75em) {
    html {
      font-size: 50%; } }
  @media only screen and (max-width: 56.25em) {
    html {
      font-size: 37.5%; } }
  @media only screen and (max-width: 37.5em) {
    html {
      font-size: 30%; } }
